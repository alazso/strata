name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: Build artifacts only; do not publish a release
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Build & Publish
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Resolve version
        id: version
        run: |
          set -euo pipefail
          GRADLE_VERSION="$(sed -n 's/^version=//p' gradle.properties | tr -d '[:space:]')"
          if [ -z "$GRADLE_VERSION" ]; then
            echo "Could not read version from gradle.properties" >&2
            exit 1
          fi
          if [ "${{ github.event_name }}" = "push" ]; then
            TAG="${GITHUB_REF_NAME}"
            TAG_VERSION="${TAG#v}"
            if [ "$TAG_VERSION" != "$GRADLE_VERSION" ]; then
              echo "Tag $TAG (${TAG_VERSION}) does not match gradle.properties (${GRADLE_VERSION})" >&2
              exit 1
            fi
          else
            TAG="v${GRADLE_VERSION}"
          fi
          echo "version=${GRADLE_VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Skip if release already published
        id: published
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="${{ steps.version.outputs.tag }}"
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          ASSET_COUNT="$(gh release view "$TAG" --json assets -q '.assets | length')"
          if [ "$ASSET_COUNT" -ge 6 ]; then
            echo "Release $TAG already has ${ASSET_COUNT} assets; skipping duplicate publish."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up JDK 25
        if: steps.published.outputs.skip != 'true'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"

      - name: Set up Gradle
        if: steps.published.outputs.skip != 'true'
        uses: gradle/actions/setup-gradle@v6

      - name: Build strata-api and strata-plugin
        if: steps.published.outputs.skip != 'true'
        run: |
          ./gradlew \
            :strata-api:build \
            :strata-plugin:build \
            :strata-api:javadocJar \
            :strata-plugin:javadocJar \
            --no-configuration-cache \
            --stacktrace

      - name: Publish strata-api to repo.alaz.so
        if: steps.published.outputs.skip != 'true' && (github.event_name == 'push' || inputs.dry_run != true)
        env:
          ALAZSO_REPO_USER: ${{ secrets.ALAZSO_REPO_USER }}
          ALAZSO_REPO_TOKEN: ${{ secrets.ALAZSO_REPO_TOKEN }}
        run: |
          ./gradlew :strata-api:publishLibraryPublicationToAlazsoRepository \
            --no-configuration-cache \
            --stacktrace

      - name: Stage release artifacts
        if: steps.published.outputs.skip != 'true'
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          STAGING="release-artifacts"
          mkdir -p "$STAGING"

          cp "strata-plugin/build/libs/strata-plugin-${VERSION}.jar" \
            "$STAGING/strata-${VERSION}.jar"
          cp "strata-api/build/libs/strata-api-${VERSION}.jar" \
            "$STAGING/strata-api-${VERSION}.jar"
          cp "strata-plugin/build/libs/strata-plugin-${VERSION}-javadoc.jar" \
            "$STAGING/strata-${VERSION}-javadoc.jar"
          cp "strata-api/build/libs/strata-api-${VERSION}-javadoc.jar" \
            "$STAGING/strata-api-${VERSION}-javadoc.jar"

          (cd "strata-plugin/build/dokka/javadoc" && \
            zip -rq "${GITHUB_WORKSPACE}/${STAGING}/strata-${VERSION}-javadoc.zip" .)
          (cd "strata-api/build/dokka/javadoc" && \
            zip -rq "${GITHUB_WORKSPACE}/${STAGING}/strata-api-${VERSION}-javadoc.zip" .)

          ls -lh "$STAGING"

      - name: Upload release artifact bundle
        if: steps.published.outputs.skip != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ steps.version.outputs.tag }}
          path: release-artifacts/*
          if-no-files-found: error

      - name: Publish GitHub Release
        if: steps.published.outputs.skip != 'true' && (github.event_name == 'push' || inputs.dry_run != true)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          TAG="${{ steps.version.outputs.tag }}"
          STAGING="release-artifacts"
          NOTES="${RUNNER_TEMP}/release-notes.md"

          {
            echo "## Strata ${VERSION}"
            echo
            echo "Version \`${VERSION}\` from \`gradle.properties\` (also injected into \`paper-plugin.yml\` at build time)."
            echo
            echo "### Artifacts"
            echo
            echo "| File | Description |"
            echo "| --- | --- |"
            echo "| \`strata-${VERSION}.jar\` | Strata Paper/Folia plugin (install in /plugins) |"
            echo "| \`strata-api-${VERSION}.jar\` | Compile-time API library for dependent plugins |"
            echo "| \`strata-${VERSION}-javadoc.jar\` | Javadoc JAR for the plugin module |"
            echo "| \`strata-api-${VERSION}-javadoc.jar\` | Javadoc JAR for the API module |"
            echo "| \`strata-${VERSION}-javadoc.zip\` | Browseable HTML Javadoc for the plugin |"
            echo "| \`strata-api-${VERSION}-javadoc.zip\` | Browseable HTML Javadoc for the API |"
          } > "$NOTES"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$STAGING"/* --clobber
          else
            gh release create "$TAG" \
              --title "Strata ${VERSION}" \
              --notes-file "$NOTES" \
              "$STAGING"/*
          fi
